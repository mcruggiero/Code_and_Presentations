response = os_client.search(
    index="hackaton_write",
    body={
        "query": {
            "match": {
                "test_case_name": "Mass Update"
            }
        }
    }
)
response["hits"]["hits"]

### 🔍 Query 1: Match on `test_case_name`
This query searches for documents where the `test_case_name` field contains the phrase `"Mass Update"`.  
It uses full-text matching, so it's flexible and returns partial matches.  
This is useful when browsing for related tests by their titles.

response = os_client.search(
    index="hackaton_write",
    body={
        "query": {
            "term": {
                "review_status": "unreviewed"
            }
        }
    }
)
response["hits"]["hits"]

### 🔒 Query 2: Filter by Exact `review_status`
This uses a `term` query to filter for documents with `review_status` exactly equal to `"unreviewed"`.  
Because `review_status` is a keyword field, this is a precise, case-sensitive match — perfect for dashboards or filters.

response = os_client.search(
    index="hackaton_write",
    body={
        "query": {
            "nested": {
                "path": "step_json",
                "query": {
                    "match_phrase": {
                        "step_json.Step Description": "Move to the next Decision Point"
                    }
                }
            }
        }
    }
)
response["hits"]["hits"]

### 🧩 Query 3: Search Within `step_json` (Nested Field)
This `nested` query looks inside `step_json`, a list of procedural steps,  
and finds any document where a step’s `Step Description` exactly includes the phrase `"Move to the next Decision Point"`.  
This is useful for finding test cases that involve specific actions or UI interactions.

response = os_client.search(
    index="hackaton_write",
    body={
        "query": {
            "nested": {
                "path": "ai_json",
                "query": {
                    "match": {
                        "ai_json.Details.Field Instructions.Field": "Portfolio Items Dashboard"
                    }
                }
            }
        }
    }
)
response["hits"]["hits"]

### 🤖 Query 4: Search AI-Generated Field Instructions
This query searches within the deeply nested `ai_json` field,  
targeting a specific instruction field (`Details.Field Instructions.Field`).  
Here, it looks for `"Portfolio Items Dashboard"` in the AI-generated step context.  
Great for understanding how the AI describes or recommends specific form fields or dashboards.

response = os_client.search(
    index="hackaton_write",
    body={
        "size": 5,
        "query": {
            "match_all": {}
        },
        "sort": [
            {"created": {"order": "desc"}}
        ]
    }
)
response["hits"]["hits"]

### 📅 Query 5: Get Most Recently Created Documents
This query returns the 5 most recently created documents using a `match_all` query and a descending `sort` on the `created` date field.  
Useful for reviewing new entries or running freshness checks during indexing or analysis.


