We’re currently joining data from three main tables: Scorecard_Detail (with metadata and notes), Scorecard_Financial_Performance_History (with monthly financial metrics), and Scorecard_RYG_Criteria (with monthly color/status codes). Each table uses Scorecard_Detail_SID as a linking key, but we also create a custom sid_key in Python that combines the SID, year, and month fields to align time-series data. The joins are currently done downstream in pandas because attempting to perform these joins in SQL — especially across large volumes and with date-matching logic — is too slow to be usable. Even moderate-size INNER JOIN operations across all three tables quickly become impractical, leading to long runtimes or timeouts. By contrast, Python gives us the flexibility to perform partial joins and fallback logic while preserving runtime efficiency.

The problem is that joining all three tables directly in SQL is prohibitively slow, and inner joins cause significant row loss due to mismatches in monthly data. For example, we observe 1,150 unique SIDs in Scorecard_Detail, 911 in Financial_Performance, and 1,325 in RYG_Criteria. A strict SQL join would result in dropping many SIDs that are essential to the analysis. To work around this, our Python code falls back to prior months (up to three months earlier) if an exact monthly match isn’t found — preserving more data for modeling.

We’re requesting a materialized view (or possibly a staging table) that pre-joins these sources with the business logic described above. This view should include the computed sid_key, all necessary joins, and optionally use SQL techniques like ROW_NUMBER() or LAG() to return the most recent available financial or color entry if the exact month isn’t present. The goal is to centralize this join logic upstream, so our modeling pipeline can rely on a single consistent dataset without having to replicate these merges manually.

If implemented, this view would significantly improve performance and data consistency across teams. It should be based on a left join from Scorecard_Detail, ensuring that no valid records are lost, even if financial or color data is missing for a given month. We're happy to share the current pandas logic or assist in validating the view once it's built.
