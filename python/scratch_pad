{
    "mappings": {
        "properties": {
            "json_column_1": {"type": "nested"},  # Nested for JSON-like structure
            "json_column_2": {"type": "nested"},
            "date_column_1": {"type": "date", "format": "yyyy-MM-dd HH:mm:ss||yyyy-MM-dd"},
            "date_column_2": {"type": "date", "format": "yyyy-MM-dd HH:mm:ss||yyyy-MM-dd"},
            "string_column_1": {"type": "text"},  # Default string type for analysis
            "string_column_2": {"type": "keyword"},  # Use keyword if exact matching is needed
            # Add more fields as needed...
        }
    }
}
