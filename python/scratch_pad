# List all aliases and their corresponding indices
aliases = os_client.indices.get_alias()
for index_name, data in aliases.items():
    for alias in data.get("aliases", {}):
        print(f"Index: {index_name} ↔ Alias: {alias}")
