query = {
    "query": {
        "term": {
            "ai_text.keyword": {
                "value": "Test String"
            }
        }
    }
}

response = client.search(index="ai_string_tests", body=query)
for hit in response["hits"]["hits"]:
    print(hit["_source"])
